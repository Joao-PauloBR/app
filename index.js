// Arrays, Objetos

let metas = ["joão?", "alô"];

console.log(metas[1] + ", " + metas[0]);

// -----

let objeto = {
    value: "Ler um livro por mês",
    checked: false
};

console.log(objeto.value + ": " + objeto.checked);

// -----

// Arrow function
const criarMeta = () => {};

// Named Function
function criarMeta() {};
// Como estão com o mesmo nome, se eu tentar rodar o código, irá dar erro. Mas como o objetivo era mostrar que são 